/*eslint-env browser*/

var coinFlip;

        for  (coinFlip =Math.random(; coinFlip <= 10; coinFlip+= Math.round) {
            
      
        }
        var @customer_id = 13;
              
    if (flipResult == choice) {
    if (flipResult == "0") {
        window.console("Heads!");
    } else {
        window.console("Tails!");
    }